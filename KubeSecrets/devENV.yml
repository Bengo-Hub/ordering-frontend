apiVersion: v1
kind: Secret
metadata:
  name: ordering-frontend-secrets
  namespace: ordering
type: Opaque
stringData:
  # Mapbox token for maps - Set via GitHub Secrets
  mapboxToken: "${MAPBOX_TOKEN}"

  # Sentry DSN for error tracking - Set via GitHub Secrets
  sentryDsn: "${SENTRY_DSN}"
